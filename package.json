{
  "name": "yarn-plugin-buck",
  "private": true,
  "main": "src/main/index.ts",
  "devDependencies": {
    "@types/node": "^12.0.0",
    "@yarnpkg/fslib": "^2.1.0",
    "@yarnpkg/builder": "^2.1.0",
    "@yarnpkg/core": "^2.1.0",
    "@yarnpkg/plugin-link": "^2.1.0",
    "typescript": "^3.9.5"
  },
  "scripts": {
    "build": "builder build plugin --no-minify"
  },
  "installConfig": {
    "pnp": true
  }
}
